# Sprint S<N> Retro — {{PROJECT_NAME}}

> Written by `/retro` at sprint close. Aggregates the live mini-retros from
> `tasks.md`, runs the backlog audit (HARD gate), and records the sprint's
> closing narrative. Copy this template to
> `docs/project/sprints/S<N>/retro.md`. Append-only once closed.
>
> Replaces the old `STATUS-archive.md` (closing prose) + `retros/sprint-S<N>.md`
> — the sprint folder IS its own archive.

_Closed YYYY-MM-DD. Theme: _(one line)_._

## Closing narrative

_(The verbatim active-state glance from `tasks.md` at close + a 1-2 sentence
post-mortem. This is the historical record for the sprint.)_

## What went well

- _(kept; reinforce)_

## What didn't — and the fix

| Finding | Fix-now or defer? | Action |
|---|---|---|
| _(process bug / friction)_ | fix-now | _(done in this commit)_ |
| _(larger item)_ | defer | _(→ backlog `## Follow-ups` as F####)_ |

## Backlog audit (HARD gate)

> `/retro` runs this and BLOCKS close on any mismatch. Cite the result:

`Backlog audit: <N>/<N> sprint rows matched · 0 mismatches`

## Candidate A-rules

> Lessons that recurred 2+ times. Operator-gated promotion via `/retro ratify`
> → lands in `.claude/rules/brain-hot.md` (A011+). Leave empty if none.

<!-- - **A0##** <rule> — surfaced in <tasks>; promote? -->

## See also

> Paths are relative to this retro's runtime home, `docs/project/sprints/S<N>/`.

- `./tasks.md` — this sprint's board + live mini-retros
- `../../backlog.md` — backlog + follow-ups (audit target)
